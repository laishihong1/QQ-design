package com.example.qq.page;import androidx.appcompat.app.AppCompatActivity;import android.content.Intent;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.EditText;import android.widget.Toast;import com.example.qq.Client.Message;import com.example.qq.DataBase.Bean;import com.example.qq.QQThread.AddFriendThread;import com.example.qq.R;import com.example.qq.ToolClass.MessageType;import com.example.qq.ToolClass.SenderAcceptGenderMessage;import com.example.qq.ToolClass.data;public class AddFriend extends AppCompatActivity {    EditText message;    Button enter,cansel,back;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.add_friend);        init();    }    private void init(){        message=findViewById(R.id.edit_message);        Message messages=(Message)getIntent().getSerializableExtra("message");        this.message.setText(messages.getGetter()+"向你发起了一条添加请求");        enter=findViewById(R.id.enter);        enter.setOnClickListener(v -> {            //返回一个同意数据到服务端，服务端返回数据给申请者            messages.setMesType(MessageType.MESSAGE_ACCEPT_ADD_FRIEND);            Bean bean = Count.getBean();            messages.setGetter(messages.getGetter());            messages.setSender(bean.getAccount());           new Thread(new AddFriendThread(messages)).start();            SenderAcceptGenderMessage.setAddMessage(messages.getGetter(),messages);        });        cansel=findViewById(R.id.cansel);        cansel.setOnClickListener(v -> {        });        back=findViewById(R.id.back);        back.setOnClickListener(v -> {            Toast toast= Toast.makeText(AddFriend.this,"已返回",Toast.LENGTH_SHORT);            toast.show();            Count count= data.getCount();            Intent intent1 =new Intent(AddFriend.this,count.getClass());            startActivity(intent1);        });    }}